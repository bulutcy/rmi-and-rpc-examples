Attention! This project is created with Anjuta IDE. So it may have some files that looks strange to you. You can head directly to src/main.c file for code.
---

This is the server program that listens port 9987.
If it receives TCP and IP messagei it writes OK and return IP, otherwise, it returns NO

The source file under: src/main.c